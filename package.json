{
  "name": "craft-codery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt-ts build",
    "start": "nuxt-ts start",
    "export": "nuxt-ts export",
    "serve": "nuxt-ts serve",
    "generate": "nuxt-ts build && nuxt-ts export",
    "deploy": "yarn generate && gh-pages -d dist -b master -r git@github.com:craftcodery/craftcodery.github.io.git"
  },
  "dependencies": {
    "@nuxt/typescript-runtime": "^0.4.10",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^1.0.3",
    "@nuxtjs/tailwindcss": "^2.0.0",
    "gh-pages": "^3.1.0"
  }
}
