{
  "name": "craft-codery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "serve": "nuxt serve",
    "generate": "nuxt build && nuxt generate",
    "deploy": "yarn generate && gh-pages -d dist -b master -r git@github.com:craftcodery/craftcodery.github.io.git -t"
  },
  "dependencies": {
    "nuxt": "^2.15.0"
  },
  "devDependencies": {
    "@nuxt/types": "^2.0.0",
    "@nuxt/typescript-build": "^2.1.0",
    "@nuxtjs/tailwindcss": "^3.4.3",
    "@tailwindcss/aspect-ratio": "^0.2.0",
    "@tailwindcss/forms": "^0.2.1",
    "@tailwindcss/typography": "^0.4.0",
    "gh-pages": "^3.1.0",
    "tailwindcss": "2.0.1-compat",
    "typescript": "~4.2"
  }
}
